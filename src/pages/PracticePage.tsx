// 問題演習ページ - Agent A が実装（スケルトン）
import { Card, Typography } from 'antd'
const { Title } = Typography

export function PracticePage() {
  return (
    <Card>
      <Title level={3}>📖 演習</Title>
      <p>問題一覧・フィルター・演習モードが表示されます（Agent A が実装予定）</p>
    </Card>
  )
}
