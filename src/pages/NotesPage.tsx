// 付箋ページ - Agent B が実装（スケルトン）
import { Card, Typography } from 'antd'
const { Title } = Typography

export function NotesPage() {
  return (
    <Card>
      <Title level={3}>🗒️ 付箋</Title>
      <p>自分の付箋・保存した付箋・タグ別一覧が表示されます（Agent B が実装予定）</p>
    </Card>
  )
}
